sudo: required
dist: trusty
language: bash
services: docker
env:
  - DOCKER_COMPOSE_VERSION=1.17.1

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin

stages:
  - deploy

before_script:
  - echo $DOCKER_PASSWORD | docker login -u $DOCKER_USER --password-stdin

script:
  - docker-compose -f docker/docker-compose.build.yml build
  - docker-compose -f docker/docker-compose.build.yml push

after_script:
  - docker images
  - echo "Push docker images done!"

#deploy_stage:
#  stage: deploy
#  script:
#      - mkdir -p ~/.ssh
#      - echo "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_rsa
#      - chmod 700 ~/.ssh/id_rsa
#      - eval "$(ssh-agent -s)"
#      - ssh-add ~/.ssh/id_rsa
#      - ssh-keyscan -H 'travis-ci.com' >> ~/.ssh/known_hosts
#      - echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config
#      - chmod +x deploy.sh
#      - REMOTE_HOST=$REMOTE_HOST REMOTE_USER=postman17 PROJECT_DIR=/opt/jeck PROJECT_PROFILE=stage source ./deploy.sh
#
#  only:
#      - master

#after_script:
#  - docker ps
